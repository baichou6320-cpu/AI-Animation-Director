interface:
  display_name: "AI Animation Director"
  short_description: "Create compact AI animation execution packages and prompts."
  default_prompt: "Use $ai-animation-director to turn this animation idea into a concise Jimeng-ready short-film execution package."

policy:
  allow_implicit_invocation: true
