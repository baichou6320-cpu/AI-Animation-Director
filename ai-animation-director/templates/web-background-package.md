# 网站背景视频执行包

## 1. 背景设定

- 用途：[网站首屏 / Hero 背景]
- 交互：[scroll_scrub / ambient_loop / normal_playback]
- 时长：[实际时长]
- 画幅：桌面 `16:9`；移动端 `[需要 / 不需要]`
- 文案安全区：[left_center / right_center / center / none]

## 2. IMG-WEB-HERO

使用方式：[即梦生图 / 即梦画布 / 已有图片]

复制提示词：

```text
[完整关键帧提示词：主体与环境、前中后景、主色和光源、材质、主视觉位置、文案安全区、桌面构图、无文字水印]
```

检查：

- 文案安全区没有高亮主体和密集细节。
- 主视觉在安全区对侧，桌面与移动裁切后仍可读。
- 没有文字、Logo、水印、签名或未授权元素。

## 3. VID-WEB-HERO

使用图片：`IMG-WEB-HERO`

复制提示词：

```text
以 IMG-WEB-HERO 为唯一画面参考。起始状态：[第一帧状态]。结束状态：[最后一帧状态]。环境运动：[最多 2-4 项轻微运动]。摄影机：[一个连续运动或固定]。始终锁定：[地形、建筑、人物位置、主体轮廓、光源方向、文案安全区]。整段为单一连续镜头，无切镜、无转场、无突然出现的新主体、无快速变焦、无强闪烁、无结构变形、无文字、无 Logo、无水印。任意时间点画面都应完整可读，适合页面滚动前进和反向查看。
```

失败降级：

```text
固定摄影机和全部静态结构，只保留[一种环境微动]与非常轻微的光影变化，画面构图和文案安全区始终不变。
```

## 4. 网页媒体交付

| 资产 | 规格 | 状态 |
| --- | --- | --- |
| `hero-desktop.mp4` | 1280×720 / H.264 / 24fps / 无音轨 | [待生成] |
| `hero-mobile.mp4` | 720×1280 / H.264 / 24fps / 无音轨 | [待生成] |
| `hero-poster.webp` | 1280×720 | [待生成] |

本地转换：

```bash
python -m production_workspace prepare-web-background SOURCE --output-directory site/public/media --poster-source IMG-WEB-HERO --duration [实际时长]
```

## 5. 失败修正

- 文案被画面抢走：[降低安全区细节、对比和高亮密度]。
- 滚动定位卡顿：[重新编码为约 0.125 秒关键帧间隔并启用 faststart]。
- 场景漂移或结构变形：[固定摄影机，只保留一个环境运动]。
