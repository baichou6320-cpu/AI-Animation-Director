# 03-stage-review.md

阶段：`director_scene` / `asset_library` / `reference_index` / `seedance_motion_prompt` / `storyboard_panel` / `sample_plan` / `seedance_prompt`

结论：`PASS` / `FAIL`

## 1. 业务审核评分
| 维度 | 分数 | 问题 | 返修建议 |
| --- | --- | --- | --- |

平均分：

最低单项：

判定规则：平均分低于 8 或任一单项低于 6 时为 `FAIL`。

## 2. 合规审核
| 项目 | 结论 | 风险 | 修正 |
| --- | --- | --- | --- |

## 3. 合并返修意见
-

## 4. 下一步
- PASS：
- FAIL：

## 5. 进化信号
如发现可泛化规则，只记录为待确认建议，不自动修改 Skill。

```json
{
  "evolution_signals": []
}
```
