# [时长] [风格] 即梦画布执行包

## 1. 先做这几步
1. 生成或导入 `ASSET-*` 素材。
2. 在 `CV-MASTER` 的 `Z-ASSET` 建立角色、场景和道具资产区。
3. 按 `CV-OP-*` 操作卡完成 `Z-S01` 到 `Z-S03`。
4. 导出 `IMG-S01` 到 `IMG-S03`。
5. 用对应导出执行 `VID-S01` 到 `VID-S03`。

## 2. 一句话设定
[一句话说明故事、角色、动作和情绪回报]

## 3. 全局锚点
- 角色：
- 场景：
- 风格：
- 避免：

## 4. 镜头表
| 镜头 | 时长 | 画面 | 动作 | 即梦方式 |
| --- | --- | --- | --- | --- |
| S01 |  |  |  | 图生视频 |
| S02 |  |  |  | 图生视频 |
| S03 |  |  |  | 图生视频 |

## 5. 画布资产与关键帧区

### 5.1 画布规划
| 画布 | 区域 | 用途 | 放置内容 | 导出 |
| --- | --- | --- | --- | --- |
| `CV-MASTER` | `Z-ASSET` | 资产母版 | `ASSET-CHAR-A`、`ASSET-SCENE-A` | `IMG-REF` |
| `CV-MASTER` | `Z-S01` | 镜头 S01 | 角色与场景构图 | `IMG-S01` |
| `CV-MASTER` | `Z-S02` | 镜头 S02 | 角色与场景构图 | `IMG-S02` |
| `CV-MASTER` | `Z-S03` | 镜头 S03 | 角色与场景构图 | `IMG-S03` |

### 5.2 素材准备

#### IMG-REF / ASSET-CHAR-A + ASSET-SCENE-A
来源：生成 / 用户导入
素材提示词：
```text
[只放生成参考素材所需的提示词]
```
完成检查：

### 5.3 画布操作

#### CV-OP-01 [镜头 S01 构图]
画布/区域：`CV-MASTER / Z-S01`
输入素材：`ASSET-CHAR-A`、`ASSET-SCENE-A`
操作类型：`arrange`
完成检查：
失败后改法：
导出为：`IMG-S01`

#### CV-OP-02 [镜头 S01 融合]
画布/区域：`CV-MASTER / Z-S01`
输入素材：`ASSET-CHAR-A`、`ASSET-SCENE-A`
操作类型：`blend`
操作提示词：
```text
[只放当前融合或局部编辑任务]
```
完成检查：
失败后改法：
导出为：`IMG-S01`

#### CV-OP-03 [镜头 S02 构图与修正]
画布/区域：`CV-MASTER / Z-S02`
输入素材：`ASSET-CHAR-A`、`ASSET-SCENE-A`
操作类型：`inpaint`
操作提示词：
```text
[只放镜头 S02 当前局部修正任务]
```
完成检查：
失败后改法：
导出为：`IMG-S02`

#### CV-OP-04 [镜头 S03 构图与修正]
画布/区域：`CV-MASTER / Z-S03`
输入素材：`ASSET-CHAR-A`、`ASSET-SCENE-A`
操作类型：`inpaint`
操作提示词：
```text
[只放镜头 S03 当前局部修正任务]
```
完成检查：
失败后改法：
导出为：`IMG-S03`

## 6. 即梦视频复制区

### VID-S01 镜头 1
推荐方式：图生视频
使用图片：`IMG-S01`
复制提示词：
```text
[只放视频提示词正文]
```
失败后改法：

### VID-S02 镜头 2
推荐方式：图生视频
使用图片：`IMG-S02`
复制提示词：
```text
[只放视频提示词正文]
```
失败后改法：

### VID-S03 镜头 3
推荐方式：图生视频
使用图片：`IMG-S03`
复制提示词：
```text
[只放视频提示词正文]
```
失败后改法：

## 7. 失败修正
- [主要失败风险 1]：[最短修正方式]
- [主要失败风险 2]：[最短修正方式]
- [主要失败风险 3]：[最短修正方式]
